# Optional. Without it, extraction falls back to keyword matching and rationale
# falls back to a template sentence. See docs/02-ENGINEERING.md section 6.
GEMINI_API_KEY=

# Only needed to re-run scripts/fetch-colleges.mjs against live Scorecard data.
# Get one instantly at https://api.data.gov/signup/
SCORECARD_API_KEY=
