# Optional. Without it, extraction falls back to keyword matching and rationale
# falls back to a template sentence. See docs/02-ENGINEERING.md section 6.
GEMINI_API_KEY=

# Used when the counselor builds a list. Without it, the app uses src/data/colleges.json
# and says so. Get a key at https://api.data.gov/signup/
SCORECARD_API_KEY=
